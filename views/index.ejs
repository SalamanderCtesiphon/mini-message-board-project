<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <ul>
      <% for (var i = 0; i < messages.length; i++) { %>
      <li style="list-style: none;">
        <div style="display: flex; flex-direction: column; padding: 10px; border: 1px solid black; margin: 10px; width: 20vw;">
          <span>
            User: <%= messages[i].user %>
          </span>
          <span>
            Message:
            <%= messages[i].text %>
          </span>
          <span>
            <%= messages[i].added %>
          </span>
            
        </div>
      </li>
      <% } %>
  </ul>
  </body>
</html>
